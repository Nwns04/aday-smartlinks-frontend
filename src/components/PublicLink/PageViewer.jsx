import React from 'react';

export default function PageViewer({ campaign }) {
  // just render an empty wrapper here – everything else in PublicLink’s return
  // will still show up underneath the editor toggle
  return null;
}
